# CryptPad Sodium plugin

CryptPad plugin that can be used to reduce CPU usage on the server by replacing the [TweetNaCl](https://www.npmjs.com/package/tweetnacl) library with [sodium-native](https://www.npmjs.com/package/sodium-native).
